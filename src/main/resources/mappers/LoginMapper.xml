<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.weavusshoppingmall.repository.UserMapper">

    <select id="findByIdAndPassword" resultType="com.example.weavusshoppingmall.entity.User">
        select * from user
        where id = #{id} AND password = #{password}
    </select>

    <insert id="saveUser" parameterType="com.example.weavusshoppingmall.entity.User">
        insert into user (id, password, user_email, card_number, card_cvc, user_name, user_en_name, is_active)
        values (#{id}, #{password}, #{userEmail}, #{cardNumber}, #{cardCvc}, #{userName}, #{userEnName}, #{isActive});
    </insert>

    <select id="findById" resultType="com.example.weavusshoppingmall.entity.User">
        select id, password, user_email as userEmail, card_number as cardNumber, card_cvc as cardCvc,
               user_name as userName, user_en_name as userEnName, created_date
        from user
        where id = #{id}
    </select>




</mapper>